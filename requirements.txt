vllm==0.17.0
tyro==0.9.35
wandb==0.23.0
weave==0.52.33
peft==0.18.0
datasets==4.4.1
# Optional, MathTask only (not used by the h1 GSM-LongHorizon run). gem-llm's
# Requires-Python is ">=3.10,<=3.12", which excludes 3.12.1+; use python_full_version
# so pip installs it only where it is actually installable and skips it otherwise.
gem-llm==0.1.0; python_full_version >= "3.10" and python_full_version <= "3.12.0"
pylatexenc==2.10
ray==2.51.1
accelerate==1.11.0
transformers==4.57.1
safetensors==0.6.2